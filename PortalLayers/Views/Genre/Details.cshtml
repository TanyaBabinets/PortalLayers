@model PortalLayers.BLL.DTO.GenreDTO

@{
    ViewData["Title"] = "Details";
}

<h1>Детали</h1>

<div>
    <h4>Жанры</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.name)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" id="buttons" asp-route-id="@Model?.Id">Изменить</a>
    <a asp-action="Index" id="buttons">Назад</a>
</div>
